# Stockman Dash app — database connection
# Either set DATABASE_URL directly...
# DATABASE_URL=postgresql+psycopg2://stockman:secret@localhost:5432/stockman

# ...or set individual DB_* vars (used if DATABASE_URL is unset)
DB_USER=stockman
DB_PASS=changeme
DB_HOST=localhost
DB_PORT=5432
DB_NAME=stockman

# App settings
DASH_DEBUG=true
PORT=8050
